//
//  ASD.swift
//  ASD Prototype
//
//  Created by Benjamin Lee on 6/21/25.
//



/*
 
let bufferWidth = CVPixelBufferGetWidth(self.pixelBuffer)
let bufferHeight = CVPixelBufferGetHeight(self.pixelBuffer)
let faceBox = VNImageRectForNormalizedRect(face.boundingBox, bufferWidth, bufferHeight)
let boxSize = 0.5 * max(faceBox.width, faceBox.height)
let paddedBoxHalfSize = boxSize * (1 + FaceExtractor.cropScale)
let paddedBoxSize = 2 * paddedBoxHalfSize

let cropRect = CGRect(
    x: faceBox.midX - paddedBoxHalfSize,
    y: faceBox.midY - boxSize,
    width: paddedBoxSize,
    height: paddedBoxSize
)
*/
